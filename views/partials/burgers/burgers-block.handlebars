<li class="burger">
	<img src="/assets/images/burgericon.jpg" alt="">
	{{burger_name}}
	{{#if devoured}}
	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
		Eat it
	</button>
	{{/if}}
</li>